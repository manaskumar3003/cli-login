# Copy to .env and adjust.

DB_USER=cliauth
DB_PASSWORD=cliauth
DB_NAME=cliauth

# Inactivity before a session expires.
SESSION_TIMEOUT=15m
# Lockout policy.
MAX_FAILED_ATTEMPTS=5
LOCKOUT_DURATION=5m
# bcrypt work factor.
BCRYPT_COST=12
# Shown in the authenticator app.
TOTP_ISSUER=cli-login
